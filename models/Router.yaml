title: Router
type: object
x-tags:
  - webhook-sender
x-examples: {}
properties:
  id:
    type: string
  name:
    type: string
    description: The name must be unique within a workspace
  tags:
    type: array
    items:
      type: string
  allowed_topics:
    type: array
    items:
      type: string
required:
  - id
